#[derive(Default)]
pub struct Other {
    pub playing_sample: bool,
    pub phase: i32,
    pub rep_time: i32,
    pub rep_limit: i32,
    pub fperiod: f64,
    pub period: i32,
    pub square_duty: f32,
}
